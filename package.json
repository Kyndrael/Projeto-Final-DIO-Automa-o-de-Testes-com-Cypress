{
  "name": "cypress-automation-project",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress open",
    "test:headless": "cypress run",
    "report": "npx mochawesome-merge cypress/reports/*.json > report.json && npx mochawesome-report-generator report.json"
  },
  "devDependencies": {
    "cypress": "^12.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^6.2.2"
  }
}
